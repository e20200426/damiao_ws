`u2canfd_cpp` folder is sample c++ code to control damiao motor using their USB-to-CANUSB.

`u2canfd_py` folder is sample python code to control damiao motor using their USB-to-CANUSB.

`tanerb_sub` package is used for subscribe \jointstates from Moveit2 (Rviz). Using Python to control the motors. 
